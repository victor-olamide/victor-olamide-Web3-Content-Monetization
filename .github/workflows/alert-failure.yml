name: Alert on Failure

on:
  workflow_run:
    workflows: ["CI Backend", "CI Frontend", "CI Blockchain", "Deploy to Production"]
    types:
      - completed

jobs:
  alert:
    if: github.event.workflow_run.conclusion == 'failure'
    runs-on: ubuntu-latest

    steps:
    - name: Send alert
      run: |
        echo "Workflow ${{ github.event.workflow_run.name }} failed"
        # Add notification logic here, e.g., send to Slack, email, etc.
## Recommendations feature (Issue #72)

- Add `UserPreference` model to capture user preferences.
- Add `recommendationService` implementing a simple heuristic ranking.
- Add `GET /api/recommendations/:userId` endpoint.
- Add seed utility to create sample user preferences.
